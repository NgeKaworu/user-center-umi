import type { FormItemProps } from 'antd/lib/form';
import type { InputProps } from 'antd/lib/input';

export default interface FormItemInput {
  formItemProps?: FormItemProps;
  inputProps?: InputProps;
}
